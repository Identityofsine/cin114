<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket Purchase Confirmation - CIN114</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Playfair:ital,opsz,wght@0,5..1200,300..900;1,5..1200,300..900&display=swap');
        
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: #000;
            color: #fff;
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
        
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #0E0E0E;
            border: 1px solid #2c2c2c;
        }
        
        .header {
            background-color: #000;
            padding: 40px 30px;
            text-align: center;
            border-bottom: 4px solid #fff;
        }
        
        .logo {
            font-family: 'Playfair', serif;
            font-size: 48px;
            font-weight: 900;
            color: #fff;
            margin-bottom: 8px;
            letter-spacing: -1px;
        }
        
        .tagline {
            font-size: 14px;
            color: #a6a6a6;
            font-weight: 400;
        }
        
        .content {
            padding: 40px 30px;
        }
        
        .success-message {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .success-title {
            font-family: 'Playfair', serif;
            font-size: 36px;
            font-weight: 700;
            color: #fff;
            margin-bottom: 12px;
        }
        
        .success-subtitle {
            font-size: 18px;
            color: #c3c3c3;
            font-weight: 500;
        }
        
        .ticket-section {
            background-color: #161616;
            border: 1px solid #2c2c2c;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
            box-shadow: 0px 4px 24px rgba(0, 0, 0, 0.25);
        }
        
        .section-title {
            font-family: 'Playfair', serif;
            font-size: 24px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 20px;
            border-bottom: 2px solid #3f3f3f;
            padding-bottom: 8px;
        }
        
        .event-info {
            margin-bottom: 25px;
        }
        
        .event-title {
            font-size: 20px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 8px;
        }
        
        .event-details {
            font-size: 14px;
            color: #a6a6a6;
            line-height: 1.5;
        }
        
        .tickets-grid {
            display: grid;
            gap: 15px;
            margin: 20px 0;
        }
        
        .ticket-item {
            background-color: #0c0c0c;
            border: 1px solid #3f3f3f;
            border-radius: 6px;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .ticket-info {
            flex: 1;
        }
        
        .ticket-number {
            font-family: 'Inter', monospace;
            font-size: 16px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 4px;
        }
        
        .ticket-type {
            font-size: 14px;
            color: #959595;
        }
        
        .ticket-price {
            font-size: 18px;
            font-weight: 600;
            color: #fff;
        }
        
        .payment-section {
            background-color: #161616;
            border: 1px solid #2c2c2c;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
        }
        
        .payment-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 15px;
        }
        
        .payment-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #2c2c2c;
        }
        
        .payment-label {
            font-size: 14px;
            color: #a6a6a6;
        }
        
        .payment-value {
            font-size: 14px;
            font-weight: 500;
            color: #fff;
        }
        
        .total-row {
            border-top: 2px solid #3f3f3f;
            padding-top: 15px;
            margin-top: 15px;
        }
        
        .total-label {
            font-size: 16px;
            font-weight: 600;
            color: #fff;
        }
        
        .total-value {
            font-size: 18px;
            font-weight: 700;
            color: #fff;
        }
        
        .next-steps {
            background-color: #161616;
            border: 1px solid #2c2c2c;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
        }
        
        .next-steps ul {
            margin-left: 20px;
            margin-top: 15px;
        }
        
        .next-steps li {
            color: #c3c3c3;
            margin-bottom: 8px;
            font-size: 14px;
        }
        
        .calendar-section {
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #3f3f3f;
        }
        
        .calendar-title {
            font-size: 16px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 15px;
        }
        
        .calendar-btn {
            background-color: #2c2c2c;
            border: 1px solid #4f4f4f;
            border-radius: 6px;
            padding: 12px 24px;
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-block;
            text-align: center;
        }
        
        .calendar-btn:hover {
            background-color: #404040;
            border-color: #666;
            color: #fff;
        }
        
        .calendar-btn:active {
            transform: translateY(1px);
        }
        
        .calendar-btn::before {
            content: "📅 ";
            margin-right: 8px;
        }
        
        .footer {
            background-color: #000;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #2c2c2c;
        }
        
        .contact-info {
            font-size: 14px;
            color: #737373;
            margin-bottom: 15px;
        }
        
        .social-links {
            margin: 20px 0;
        }
        
        .social-links a {
            color: #a6a6a6;
            text-decoration: none;
            margin: 0 15px;
            font-size: 14px;
            transition: color 0.3s ease;
        }
        
        .social-links a:hover {
            color: #fff;
        }
        
        .copyright {
            font-size: 12px;
            color: #5c5c5c;
            margin-top: 20px;
        }
        
        /* Mobile responsiveness */
        @media (max-width: 600px) {
            .email-container {
                margin: 0;
                border: none;
            }
            
            .header, .content, .footer {
                padding: 20px 15px;
            }
            
            .success-title {
                font-size: 28px;
            }
            
            .payment-grid {
                grid-template-columns: 1fr;
            }
            
            .ticket-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }
            
            .calendar-btn {
                width: 100%;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <div class="logo">CIN114</div>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <!-- Success Message -->
            <div class="success-message">
                <h1 class="success-title">Purchase Confirmed</h1>
                <p class="success-subtitle">Your tickets have been successfully purchased</p>
            </div>
            
            <!-- Event Information -->
            <div class="ticket-section">
                <h2 class="section-title">Event Details</h2>
                <div class="event-info">
                    <div class="event-title">{{.EventTitle}}</div>
                    <div class="event-details">
                        <strong>Date:</strong> {{.EventDate}}<br>
                        <strong>Time:</strong> {{.EventTime}}<br>
                        <strong>Venue:</strong> {{.EventVenue}}<br>
                        <strong>Address:</strong> {{.EventAddress}}
                    </div>
                </div>
                
                <!-- Add to Calendar Section -->
                <div class="calendar-section">
                    <h3 class="calendar-title">Add to Your Calendar</h3>
                    <p style="font-size: 14px; color: #a6a6a6; margin-bottom: 15px;">Download the event details to your calendar app</p>
                    <a href="{{.CalendarURL}}" download="cin114-event.ics" class="calendar-btn">Add to Calendar</a>
                </div>
            </div>
            
            <!-- Tickets -->
            <div class="ticket-section">
                <h2 class="section-title">Your Tickets</h2>
                <div class="tickets-grid">
                    {{range .Tickets}}
                    <div class="ticket-item">
                        <div class="ticket-info">
                            <div class="ticket-number">Ticket #{{.Number}}</div>
                            <div class="ticket-type">{{.Type}}</div>
                        </div>
                        <div class="ticket-price">${{.Price}}</div>
                    </div>
                    {{end}}
                </div>
            </div>
            
            <!-- Payment Summary -->
            <div class="payment-section">
                <h2 class="section-title">Payment Summary</h2>
                <div class="payment-grid">
                    <div class="payment-item">
                        <span class="payment-label">Subtotal:</span>
                        <span class="payment-value">${{.Subtotal}}</span>
                    </div>
                    <div class="payment-item">
                        <span class="payment-label">Processing Fee:</span>
                        <span class="payment-value">${{.ProcessingFee}}</span>
                    </div>
                    <div class="payment-item">
                        <span class="payment-label">Tax:</span>
                        <span class="payment-value">${{.TaxAmount}}</span>
                    </div>
                    <div class="payment-item">
                        <span class="payment-label">Payment Method:</span>
                        <span class="payment-value">{{.PaymentMethod}}</span>
                    </div>
                </div>
                <div class="payment-item total-row">
                    <span class="total-label">Total Paid:</span>
                    <span class="total-value">${{.TotalAmount}}</span>
                </div>
                <div class="payment-item">
                    <span class="payment-label">Transaction ID:</span>
                    <span class="payment-value">{{.TransactionID}}</span>
                </div>
                <div class="payment-item">
                    <span class="payment-label">Purchase Date:</span>
                    <span class="payment-value">{{.PurchaseDate}}</span>
                </div>
            </div>
            
            <!-- Next Steps -->
            <div class="next-steps">
                <h2 class="section-title">What's Next?</h2>
                <ul>
                    <li>Save this email as your receipt and ticket confirmation</li>
                    <li>Arrive at the venue 30 minutes before the screening</li>
                    <li>Present this email or your ticket numbers at the door</li>
                    <li>Contact us if you have any questions or need assistance</li>
                </ul>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="contact-info">
                For questions about your purchase, contact us at<br>
                <strong>contact@cin114.net</strong>
            </div>
            
            <div class="social-links">
                <a href="https://www.youtube.com/@CIN114">YouTube</a>
                <a href="https://www.instagram.com/cin114films">Instagram</a>
                <a href="https://x.com/CIN114films">Twitter</a>
                <a href="https://vimeo.com/user223422993">Vimeo</a>
                <a href="https://www.patreon.com/CIN114">Patreon</a>
            </div>
            
            <div class="copyright">
                © 2024 CIN114. All rights reserved.<br>
                A production company built for artists, by artists.
            </div>
        </div>
    </div>
</body>
</html> 
